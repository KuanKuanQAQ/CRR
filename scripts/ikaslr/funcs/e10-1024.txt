# 本档暂缺。
#
# E10 的范围扫描要求各档之间有包含关系（同一排序取前 N 个），而当前已清理的
# s3-full 只有 483 个函数——1024 这一档需要把候选范围扩大（例如加入 kernel/、
# drivers/ 的部分子系统）之后，重新跑一遍"编译 → verify_movable → 剔除 → 重编"
# 的迭代才能得到。未做之前留空，不放一份口径不一致的名单进来。
#
# 生成办法见 scripts/ikaslr/gen_funcs.py 与 实现/15-scope-selection.md。
