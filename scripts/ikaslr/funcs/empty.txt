# 空名单：Base 档专用。
#
# 为什么 Base 也要加载编译器插件：清单 §0.1 的纪律是"四档之间只允许差 IKASLR
# 相关 CONFIG，编译器全部一致"。若 Base 干脆不加 -fpass-plugin，两档的编译流水线
# 就不同了，测出来的差额里混着"加载插件本身"的影响。
#
# 用空名单则插件照样加载、照样跑，但一个函数都不改造（pass 在名单为空时立即返回
# PreservedAnalyses::all()），于是两档的唯一差别就是 CONFIG_IKASLR 与被随机化的
# 那批函数——正是我们要测的自变量。
