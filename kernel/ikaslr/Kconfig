# SPDX-License-Identifier: GPL-2.0-only
config IKASLR
	bool "I-KASLR: continuous re-randomization of built-in kernel code"
	depends on X86_64 || ARM64
	help
	  面向 built-in 内核代码的持续随机化（论文第 3 章）。以固定位置的跳板函数
	  集中代码索引，使一次随机化只需更新 O(1) 个索引；以跳板进出计数精确追踪
	  随机化区域内的执行流，实现无副本随机化。

	  这是 I-KASLR 系统的执行机制层，第 4 章的按需触发与第 5 章的 PA 精准 CFI
	  在其之上展开。

	  实验/研究用途。若不确定，选 N。

config IKASLR_STATS
	bool "I-KASLR runtime statistics"
	depends on IKASLR
	default y
	help
	  在跳板热路径上累计进入次数、最大并发数等统计量。

	  实测这些统计占了跳板开销的绝大部分（§3.6.3），因此测量"生产配置"下的
	  跳板开销时应关掉本项；它与 IKASLR_DEBUG 独立，正是为了能在开着调试
	  接口（/proc/ikaslr/bench）的同时关掉统计。

config IKASLR_DEBUG
	bool "I-KASLR debugging output"
	depends on IKASLR
	help
	  打开 I-KASLR 各阶段的诊断输出（区域边界、指针表、随机化时间分解等）。

config IKASLR_EVAL_VULN
	bool "I-KASLR evaluation-only vulnerable module (arbitrary kernel R/W)"
	depends on IKASLR && IKASLR_DEBUG
	help
	  编译进一个**仅用于评估**的含漏洞接口，经 /proc/ikaslr/attack 向用户态
	  暴露任意内核地址读与写两项原语（论文 §4.6.1 的"攻击者能力载体"）。

	  它实例化威胁模型假设攻击者已具备的任意读写能力，用于在受控的 QEMU 环境
	  中验证检测与随机化的响应。约束（§4.6.1）：只提供任意读写、不额外泄露布局、
	  不提供代码执行、位于非随机化区域、不参与性能测量。

	  ⚠ 这是一个**故意开的内核后门**。它把整个内核内存暴露给普通用户态进程，
	  绝不能进入任何可部署的构建。加载时会污染内核（TAINT）。默认且必须为 N。

	  If unsure, say N.

config IKASLR_XOM_EPT
	bool "I-KASLR x86 EPT-based execute-only memory (nested hypervisor)"
	depends on IKASLR && X86_64
	help
	  第 4 章 x86 平台的只执行内存路径（论文 §4.5.1）。内核启动一层薄
	  hypervisor 把自身降为 guest，用二级地址翻译（EPT）把代码物理页设为
	  不可读；攻击者读代码页触发 EPT violation，被捕获并触发随机化。

	  作用于物理页面，因而天然覆盖该页的全部虚拟映射，避免以虚存页权限实现
	  XOM 的多重映射绕过。

	  需要 CPU 支持 VMX+EPT，且在 QEMU 下需要开启嵌套虚拟化（kvm nested=Y）。
	  这是一个精细的底层机制，分步实现（见 04-detection.md）。研究用途，
	  默认 N。
